import 'package:flutter/material.dart';

class MyMainPageDesktop extends StatelessWidget {
  const MyMainPageDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Main Page Desktop'),
      ),
    );
  }
}
