import 'package:flutter/material.dart';

class MyMainPageTablet extends StatelessWidget {
  const MyMainPageTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Main Page Tablet'),
      ),
    );
  }
}
